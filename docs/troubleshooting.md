# Troubleshooting

- **ESP32 not connecting to Wi-Fi**: Check credentials in config.cpp.
- **Commands not executing**: Ensure ESP32 is polling the commands table and Supabase URL is correct.
- **Sensors show 0**: Verify GPIO pins in config.h.
